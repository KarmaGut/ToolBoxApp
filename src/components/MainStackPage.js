import React, { Component } from "react";
import { View, Text } from "react-native";

export default class MainStackPage extends Component {
    render () {
        return (
            <View style={{ flex: 1 }}>
                <Text>这是主页面</Text>
            </View>
        )
    }
}